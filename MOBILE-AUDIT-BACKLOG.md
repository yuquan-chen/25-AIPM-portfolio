# AIPM 作品集手机端体验审计 Backlog

审计日期：2026-08-23
审计范围：首页顶部至页脚、主导航、浮动目录、城市可听化笔记、Finance Agent、Companion Agent、Editing Agent、LLM Learning Journey、About/Contact、外链与 PDF、iframe 内交互、控制台日志。

## 环境与限制

- 桌面端：CSS 视口 `1440 x 900`。
- 手机端：浏览器仅提供 Chromium 内核；没有可调用的 WebKit/Safari 实例。
- 应用内浏览器的最小内容视口实际为 `466 x 784`，即使请求 `390 x 844` 也会被宿主限制，未能完成真实 `390 x 844` 和真机 Safari 验证。
- 本文件只记录问题，不包含本轮代码修复。

## 统计

- 总问题数：12
- P0：0
- P1：3
- P2：6
- P3：3
- 共同根因：`R1` 浮动目录定位，`R2` iframe 固定高度/嵌套溢出，`R3` 移动端触控热区，`R4` 城市笔记翻页后的滚动定位，`R5` Finance 地图移动端信息密度，`R6` 锚点与外部依赖/日志。

## 问题总表

| 编号 | 页面或组件 | 问题描述 | 复现步骤 | 手机端实际表现 | 桌面端对照表现 | 影响 | 问题类型 | 严重程度 | 截图或其他证据 | 可能原因 | 建议解决方向 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| P1-01 | 首页固定目录（`R1`） | 浮动“目录”卡覆盖正文，不随窄屏内容避让 | 手机端打开 `#top`、`#about` | `466px` 视口下目录 `x=12..62,y=72..154`；首页 `h1` 的 `x=34..62,y=113..154` 与其相交；About 第一段也相交，左侧文字被遮住 | `1440px` 下目录位于留白区，未遮挡正文 | 核心标题/段落无法完整阅读 | 布局/遮挡 | P1 | 手机端顶部、About 截图；DOM 矩形相交数据 | 固定定位和 `z-index:40` 未按窄屏内容区重新定位 | 为窄屏设置避让区域、随章节/滚动调整位置，或缩小/改变目录停靠点 | 待修复 |
| P1-02 | Companion Agent iframe（`R2`） | Demo 内容高度超过 iframe，底部感知面板与建议不可达 | 手机端进入 Companion Agent | iframe 高 `1010px`，内部 `scrollHeight=1467.76px`，`body.embedded` 为 `overflow:hidden`；感知指标从约 `968px` 开始，图表/建议继续到 `1325px+` | 桌面端 iframe 高 `690px`，内部高度可容纳 | 核心“感知→介入→建议”流程在手机端被截断，无法完整操作 | iframe/裁切 | P1 | 进入 Demo 后的 iframe 高度与内部高度数据 | 外层固定高度 + 嵌入态禁止滚动 | 手机端让 iframe 高度随内容增长，或提供受控内部滚动并保证整段可达 | 待修复 |
| P1-03 | Editing Agent iframe（`R2`） | 手机端工作台下半段被 iframe 裁切 | 手机端进入 Editing Agent | iframe 高 `1600px`，内部 `scrollHeight=1911.86px`；`timeline-wrap` 从约 `1648px` 开始，`sync-note` 结束于 `1891.86px`，无法在 iframe 内看到 | 桌面端也有轻微底部裁切，但主要工作区仍可见 | 时间线和“SECOND CUT”证据区无法完整阅读/操作 | iframe/裁切 | P1 | 内部元素超出 iframe 的矩形数据 | 嵌入态 `overflow:hidden` 与固定 iframe 高度叠加 | 重新计算嵌入高度，或允许 iframe/内部工作区在手机端滚动 | 待修复 |
| P2-01 | 城市可听化笔记翻页（`R4`） | 翻页后没有把新 spread 顶部带回视口 | 打开笔记，点击第 2 页右下角下一页；再点击第 4 页下一页 | spread 2 时 `scrollY≈718`，第 3 页在视口上方，第 4 页才出现在视口；spread 3 同样先看到封底，左页在上方 | 桌面端点击后 `scrollY≈212.5`，第 3/4 页顶部约 `-80px`，上半部分被裁 | 阅读顺序断裂，用户需要手动向上滚回新页面顶部 | 交互/滚动 | P2 | 手机/桌面翻页截图；页面矩形和 `scrollY` 数据 | 点击旧页面按钮触发了浏览器自动滚动，`setCitySpread` 没有重置滚动位置 | 翻页动画结束后将新 spread 首页滚到视口顶部，并保留焦点 | 待修复 |
| P2-02 | 城市笔记关闭按钮（`R1`） | 手机端浮动目录覆盖红色关闭圆点 | 打开城市笔记，点击左上角红点 | 关闭圆点约 `x=17..31,y=116..130`，被目录 `x=12..62,y=72..154` 覆盖；直接点红点无法关闭，点页面正文或按 Escape 才能关闭 | 桌面端红点不被目录覆盖，可正常关闭 | 主要关闭入口失效，触控路径不符合视觉提示 | 交互/遮挡 | P2 | 红点与目录矩形重叠数据；触控点击未关闭、正文点击可关闭 | 目录层级高于书本，且书本关闭点没有窄屏避让 | 打开笔记时暂时隐藏/移动目录，或为书本控制层提供更高层级和独立热区 | 待修复 |
| P2-03 | 全站移动端触控控件（`R3`） | 多个可操作元素小于推荐 `44px` 触控热区 | 手机端逐项触摸导航、外链、笔记箭头和 Demo 工具 | 例：项目外链文字高约 `14.7px`；主导航约 `29.5px`；城市箭头 `30px`；Finance 缩放 `30px`；Companion 建议约 `39.6px`；Editing 播放 `30px`、重启 `35px` | 鼠标操作可用，尺寸问题不明显 | 容易误触、点不中，尤其是 iframe 和翻页控件 | 可用性/触控 | P2 | 各组件 DOM `getBoundingClientRect()` 数据 | 复用桌面文字按钮样式，没有统一移动热区 | 为移动端增加 padding/min-height，保持视觉尺寸不变但扩大 hit area | 待修复 |
| P2-04 | Finance Agent 手机地图（`R5`） | 系统地图在窄屏被压缩，文字和区域边界过密 | 手机端进入 Finance Agent，查看地图 | 地图区域约 `432 x 360`，SVG `viewBox=1120 x 760`；9px/19px SVG 字体缩放后约 3.5/7.2px，标签难读；需依赖缩放按钮 | 桌面端地图宽约 `1370px`，标签可读 | 核心运行拓扑难以检查，降低 Demo 的解释价值 | 可读性/响应式 | P2 | 手机端 Finance 截图；map-panel 与 viewBox 数据 | 固定 viewBox 等比例缩放，移动端没有独立的简化图/默认放大策略 | 移动端改为分区卡片/可聚焦节点，或提供更明显的放大与节点详情入口 | 待修复 |
| P2-05 | Finance Agent 手机左侧分区导航（`R5`） | 桌面端 Zone 导航在手机端被完全隐藏 | 手机端进入 Finance Agent，查看左侧导航 | `@media(max-width:1120px)` 下 `.left-panel` 为 `display:none`，6 个 `zone-button` 矩形为 `0 x 0`；只能直接点 SVG 边界 | 桌面端左侧 Zone 列表可见且可点击 | 结构化分区入口消失，地图操作不易发现 | 响应式/交互 | P2 | `zone-button` 计算矩形为 0；桌面/手机 CSS 对照 | 仅用隐藏桌面侧栏换取宽度，没有补充移动替代控件 | 在地图上方/下方提供可横向或纵向滚动的 Zone 列表，或把节点详情入口显式化 | 待修复 |
| P2-06 | Editing Agent 桌面 iframe（`R2`） | 工作台底部证据提示超出 iframe | 桌面端进入 Editing Agent，滚到 Demo 底部 | iframe 高 `1080px`，内部 `scrollHeight=1125px`；`sync-note` 底部约 `1097px`，嵌入视口看不到完整内容 | 同样发生，主工作区可用但底部被截 | 证据说明不完整，页面收尾被裁 | iframe/裁切 | P2 | 内部 `sync-note` 超出 iframe 的数据 | 桌面嵌入高度固定为 `1080px`，body 仍禁止滚动 | 增加桌面 iframe 高度或让底部说明进入可滚动区域 | 待修复 |
| P3-01 | 首页 `#top` 锚点 | “回到开头/品牌”链接会把导航栏滚出视口 | 点击身份链接或目录 `TOP` | `#top` 定位到 `main`：手机 `scrollY≈80`、桌面 `scrollY≈102`，`site-nav` 位于视口上方 | 同样发生 | 回到首页后看不到站点导航，需要再次向上滚 | 导航/锚点 | P3 | 无 hash 时 `scrollY=0`；`#top` 时 nav top 为 `-80/-102` | `id=top` 挂在 main 而不是页面最上方 | 将锚点放到页面顶端，或让 TOP 使用明确的 `window.scrollTo(0,0)` | 待修复 |
| P3-02 | 首页控制台 | 新首页会话出现 MutationObserver 参数错误 | 新建首页标签，等待约 1 秒，读取 console | 出现一次 `Failed to execute 'observe' on 'MutationObserver': parameter 1 is not of type 'Node'`；视觉功能仍可用 | 桌面端同样出现 | 影响调试可信度，可能掩盖后续交互错误 | 稳定性/日志 | P3 | 新标签页 console；独立直接打开 Editing Agent 页面未复现 | 错误来源 URL 未由浏览器日志暴露，可能来自主页面/嵌入脚本或宿主注入 | 先定位来源脚本和具体 observer，再补空节点保护或移除重复 observer | 待定位 |
| P3-03 | Finance Agent 飞书外链 | 外链正文可打开，但第三方页面有权限错误日志 | 点击/直接打开 Finance Agent 飞书链接 | 页面标题和正文可读；飞书脚本产生 `Uncaught (in promise) Error: PermFail` 两次 | 未发现本地页面布局问题 | 外链依赖第三方权限/会话，可能造成用户误判为链接失效 | 外部依赖/日志 | P3 | 飞书页 DOM 可读；console `PermFail`，来源为飞书 CDN | 第三方权限/未登录状态，不是本地页面渲染错误 | 发布前用未登录和已登录状态各确认一次；必要时补充外链访问说明 | 外部依赖 |

## 本轮修复状态

- P1-01、P1-02、P1-03：已修复并完成手机端回归。
- P2-01、P2-02、P2-05、P2-06、P3-01：已修复并完成回归。
- P2-03：已扩大主要移动端触控热区；Finance 地图节点本身仍需真机确认。
- P2-04：已补回手机 Zone 导航并提高地图操作按钮尺寸；地图标签密度仍属于后续信息设计问题。
- P3-02：本地页面仍会出现一次 MutationObserver 控制台错误，来源未能从浏览器日志定位；独立 Editing Agent 页面无此错误。
- P3-03：外部飞书权限日志，保持发布前环境确认项。

## 推荐解决顺序

1. **先修 P1 iframe 裁切**：Companion、Editing 的手机端内容必须完整可达；同时处理桌面 Editing 底部裁切。
2. **再修 P1/P2 浮动目录层级**：先保证正文不被遮挡，再保证城市笔记关闭点可触达。
3. **修城市笔记翻页后的滚动定位**：每次翻页后将新 spread 顶部带回视口。
4. **统一移动端触控热区**：项目外链、城市箭头、Finance 缩放、Agent 播放/建议按钮。
5. **重做 Finance 移动地图信息层级**：补回 Zone 入口并降低 SVG 信息密度。
6. **最后处理锚点与日志**：修 `#top`，定位 MutationObserver 来源；外链 PermFail 作为发布前环境确认项。

## 需要真机 / WebKit 进一步确认

- `390 x 844` 下浮动目录与标题、城市关闭点的实际遮挡范围。
- iOS Safari 的 `position:fixed`、安全区、地址栏动态高度对目录和 iframe 高度的影响。
- iOS Safari 对 `body.embedded { overflow:hidden }`、iframe 内部内容裁切及触控滚动的实际表现。
- iOS Safari 对 SVG 地图文本缩放、点击 Zone 边界和 `backdrop-filter` 的表现。
- Email/WeChat 复制权限、外部 PDF/GitHub/飞书打开行为。
